<?xml version="1.0" encoding="utf-8"?>

<Page
	x:Class="CheckoutStuff.Pages.Admin.CategoryEditPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:CheckoutStuff.Pages.Admin"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:config="using:CheckoutStuff.Configuration"
	mc:Ignorable="d">

	<Grid ColumnDefinitions="2*,3*" Padding="12">
		<Grid RowDefinitions="Auto,*,Auto" Margin="0,0,12,0">

			<TextBlock Text="Kategorie" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8" />
			<ItemsView Grid.Row="1" SelectionMode="Single" IsItemInvokedEnabled="True"
			           ItemInvoked="CategoryList_OnItemInvoked" ItemsSource="{x:Bind viewModel.Groups, Mode=OneWay}">
				<ItemsView.ItemTemplate>
					<DataTemplate x:DataType="config:ProductGroup">
						<ItemContainer>
							<StackPanel Orientation="Horizontal" Spacing="12">
								<Image Source="{x:Bind Image}" Width="48" Height="48" />
								<TextBlock Text="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center" FontSize="16" />
							</StackPanel>
						</ItemContainer>
					</DataTemplate>
				</ItemsView.ItemTemplate>
			</ItemsView>

			<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Spacing="8" Margin="0,8,0,0">
				<Button Content="Nowe" Command="{x:Bind viewModel.AddCommand}" />
				<Button Content="Usuń" Command="{x:Bind viewModel.DeleteCommand}" />
				<Button Content="Zapisz" Command="{x:Bind viewModel.SaveCommand}" />
				<Button Content="Otwórz folder ze zdjęciami" Click="OpenFolderButton_OnClick" />
			</StackPanel>
		</Grid>

		<StackPanel Grid.Column="1">
			<TextBlock Text="Szczegóły Kategorii " FontSize="20" FontWeight="SemiBold" Margin="0,0,0,8" />
			<StackPanel Orientation="Horizontal" Spacing="12" Margin="0,6">
				<Image Source="{x:Bind viewModel.SelectedGroup.Image, Mode=OneWay}" Width="128" Height="128" />
				<StackPanel>
					<TextBlock Text="Nazwa" Margin="0,0,0,4" />
					<TextBox Text="{x:Bind viewModel.SelectedGroup.Name, Mode=TwoWay}" Width="300" />

					<TextBlock Text="Plik zdjęcia" Margin="0,12,0,4" />
					<TextBox Text="{x:Bind viewModel.SelectedGroup.ImageName, Mode=TwoWay}" Width="300" />
				</StackPanel>
			</StackPanel>
		</StackPanel>
	</Grid>
</Page>